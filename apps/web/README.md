# WhatsApp CRM — Web (Frontend)

This is the Next.js frontend for the WhatsApp CRM project.

## Features (initial setup)
- Next.js configured
- TailwindCSS ready
- Base project structure created

## Commands
Install dependencies:

```bash
npm install
```

Run the development server:

```bash
npm run dev
```

The app will run on **http://localhost:3000**
